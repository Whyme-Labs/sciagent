# Literature Searcher — Subagent Prompt Template

Fill in all `[PLACEHOLDER]` fields before dispatching. Model: fastest available (mechanical search + extraction); omit the model parameter if the session offers no choice. Dispatch one searcher per source, in parallel.

```
Agent tool:
  subagent_type: general-purpose
  description: "Literature search: [SOURCE_NAME]"
  prompt: |
    You are a literature research assistant. You cannot ask questions mid-task: if anything below is unclear or a required tool is unavailable, STOP and return Status: NEEDS_CONTEXT (or BLOCKED) with specifics. Do not guess.

    ## Objective

    Search [SOURCE_NAME] for papers relevant to the research problem below, extract structured information, and write it to a JSON file.

    ## Research Problem

    [PASTE THE IDEA DNA: problem, assumption, novelty claim.
    FOR PHASE 0a IDEATION SWEEPS: paste the inspiration brief instead — the topic, what the user finds exciting, and their constraints — and add: "This is an exploratory sweep: prioritize recent surveys, the most-cited recent work, and papers reporting benchmark tables with SOTA numbers and public code."]

    ## Search Queries

    [PASTE THE 5-15 SEARCH QUERIES GENERATED BY THE ORCHESTRATOR]

    Strategy — interview the literature, don't just fan out queries:
    1. Run the broad queries first and read what comes back.
    2. Then generate follow-up queries conditioned on what you actually found — the surprising result, the repeatedly-cited prior work, the contradiction between two papers. Follow-ups reach material the initial queries never could.
    3. Budget roughly half your search effort for follow-ups.

    ## Tools and Sources

    Use [SOURCE_TOOL_NAME] to search. This is your ONLY search tool — do not use others. Prefer authoritative sources (published papers, arXiv PDFs) over blog posts and SEO content.

    ## Boundaries

    - Find at most [N] relevant papers (hard cap — quality over quantity). Prioritize:
      1. Papers directly addressing the same problem
      2. Papers proposing techniques relevant to our approach
      3. Recent survey papers in the domain
      4. Papers establishing the baselines we need to beat
    - Prefer recent papers (last 3 years) but include seminal older work if foundational.
    - Do not follow tangents outside the queries. Do not search endlessly for sources that may not exist — if a query returns nothing after 2-3 variations, record that and move on.

    ## Known pitfalls in this project

    [PASTE THE learnings ARRAY FROM state.json, OR "None yet."]

    ## Output Contract

    The orchestrator wants more than a method summary — it reads each paper for **motivation and decisions**, the substantive research behind the surface artifact. Extract both layers (see the decision-archaeology fields below).

    Write your findings to `research-log/lit/[SOURCE_NAME].json` as a JSON array. For each paper:

    ```json
    {
      "title": "Full paper title",
      "authors": "First Author et al., Year",
      "venue": "Conference/Journal name",
      "url": "Resolvable link (arXiv, DOI, publisher) — REQUIRED, entries without a working URL will be discarded",
      "source_type": "peer-reviewed | preprint | other",
      "core_contribution": "1-2 sentence summary",
      "methodology": "Brief description of the approach",
      "key_results": "Specific metric values on specific benchmarks",
      "limitations": "Limitations acknowledged by the AUTHORS",
      "math_foundations": "Key theorems, proofs, or frameworks used",
      "motivation": "What did the authors actually care about? Infer from the framing, chosen benchmark, and highlighted failure cases — NOT just the abstract's stated motivation. One paragraph.",
      "constraints_inferred": "What could the authors plausibly NOT do (compute budget, data access, prior commitments, reviewer expectations)? These often explain methodological choices. State explicitly that these are inferred.",
      "key_decisions": "Why this baseline, this benchmark, this metric, this scale, this framing? For the 2-3 most consequential decisions, note what would have changed if the authors had chosen otherwise.",
      "tried_and_discarded": "What did the authors try and abandon? Visible in ablations, footnotes, supplementary, or implied by the shape of what they kept. Mark 'not visible' if you cannot tell.",
      "load_bearing_assumptions": "Which assumption, if wrong, would invalidate the whole paper? Usually NOT the assumption the authors emphasize. Identify it explicitly.",
      "relevance": "high|medium|low",
      "relevance_reason": "Why this paper matters for our research"
    }
    ```

    Rules:
    - Only include papers you actually found and opened. Do NOT fabricate papers — every entry will be spot-checked against its URL, and one fabricated entry discards your entire batch.
    - If a search returns no results, report that — do not invent alternatives.
    - "key_results" MUST include specific numbers (e.g., "93.2% accuracy on GLUE"), not vague claims.
    - "motivation", "constraints_inferred", "key_decisions", "load_bearing_assumptions" are YOUR inferences — clearly framed as such. Do not pretend the authors stated them.
    - For papers marked `relevance: low`, you may abbreviate the inference fields to one line each. For `high` and `medium`, fill them out properly.

    ## Report (your return message — keep under ~1,500 tokens; do NOT paste the JSON)

    - **Status:** DONE / NEEDS_CONTEXT / BLOCKED
    - **File written:** research-log/lit/[SOURCE_NAME].json
    - **Papers found:** count, with counts by relevance (high/medium/low)
    - **Top 5 highlights:** one line each — title, year, why it matters
    - **Search coverage:** which queries returned results and which didn't
    - **Notes:** observations (e.g., "this area is very active", "almost nothing on this specific combination")
```
